[tool.poetry]
name = "dottree"
version = "0.1.1"
description = ""
authors = ["Jitender <gitbackspacer@gmail.com>"]

[tool.poetry.dependencies]
python = "^2.7"

[tool.poetry.dev-dependencies]
pytest = "^4.6"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
